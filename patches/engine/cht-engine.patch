diff --git a/project.b b/project.b
index f83a553..b3f2c0c 100644
--- a/project.b
+++ b/project.b
@@ -144,6 +144,7 @@ exe %xu4 [
 		%combat.cpp
 		%controller.cpp
 		%context.cpp
+		%cht.cpp
 		%creature.cpp
 		%death.cpp
 		%debug.cpp
diff --git a/src/Makefile.common b/src/Makefile.common
index 6736af1..ac16f35 100644
--- a/src/Makefile.common
+++ b/src/Makefile.common
@@ -22,6 +22,7 @@ CXXSRCS=\
         combat.cpp \
         controller.cpp \
         context.cpp \
+        cht.cpp \
         creature.cpp \
         death.cpp \
         debug.cpp \
diff --git a/src/gpu_opengl.cpp b/src/gpu_opengl.cpp
index 882a30c..29a699e 100644
--- a/src/gpu_opengl.cpp
+++ b/src/gpu_opengl.cpp
@@ -574,7 +574,7 @@ const char* gpu_init(void* res, int w, int h, int scale, int filter)
 
     // Create screen, white, noise & shadow textures.
     glGenTextures(TEXTURE_COUNT, &gr->screenTex);
-    gpu_defineTex(gr->screenTex, 320, 200, NULL,
+    gpu_defineTex(gr->screenTex, 640, 400, NULL,
 #if defined(ANDROID) || defined(USE_GLES)
                   GL_RGBA,  // Must match glTexSubImage2D format.
 #else
@@ -660,7 +660,7 @@ const char* gpu_init(void* res, int w, int h, int scale, int filter)
 
         glUseProgram(sh);
         glUniform2f(mmap, (float) w, (float) h);
-        glUniform2f(gr->slocScDim, 320.0f, 200.0f);
+        glUniform2f(gr->slocScDim, 640.0f, 400.0f);
         glUniform1i(cmap, GTU_CMAP);
     }
     }
diff --git a/src/imagemgr.cpp b/src/imagemgr.cpp
index a7cd166..88c3ff4 100644
--- a/src/imagemgr.cpp
+++ b/src/imagemgr.cpp
@@ -609,6 +609,28 @@ ImageInfo* ImageMgr::load(ImageInfo* info) {
     unscaled->save(out2.append("-fixup.ppm").c_str());
 #endif
 
+    /* U4 繁中化 B:全美術 2x(640×400 regime;RGBA 保留來源像素)*/
+    {
+        int sw = unscaled->width(), sh = unscaled->height();
+        Image* big = Image::create(sw * 2, sh * 2);
+        for (int yy = 0; yy < sh; ++yy)
+            for (int xx = 0; xx < sw; ++xx) {
+                RGBA c; unscaled->getPixel(xx, yy, c);
+                big->putPixel(xx*2,   yy*2,   c.r, c.g, c.b, c.a);
+                big->putPixel(xx*2+1, yy*2,   c.r, c.g, c.b, c.a);
+                big->putPixel(xx*2,   yy*2+1, c.r, c.g, c.b, c.a);
+                big->putPixel(xx*2+1, yy*2+1, c.r, c.g, c.b, c.a);
+            }
+        delete unscaled;
+        unscaled = big;
+        info->width  *= 2;
+        info->height *= 2;
+        SubImage* si = (SubImage*) info->subImages;
+        for (int i = 0; i < info->subImageCount; ++i) {
+            si[i].x *= 2; si[i].y *= 2; si[i].width *= 2; si[i].height *= 2;
+        }
+    }
+
     info->image = unscaled;
     //info->tex = gpu_makeTexture(info->image);
 
diff --git a/src/intro.cpp b/src/intro.cpp
index acdf1f7..f92d0cd 100644
--- a/src/intro.cpp
+++ b/src/intro.cpp
@@ -20,6 +20,7 @@
 #include "u4file.h"
 #include "utils.h"
 #include "xu4.h"
+#include "cht.h"      // U4 繁中化
 
 #ifdef GPU_RENDER
 extern bool loadMapData(Map *map, U4FILE *uf, Symbol borderTile);
@@ -33,9 +34,12 @@ using namespace std;
 #define INTRO_MAP_HEIGHT 5
 #define INTRO_MAP_WIDTH 19
 #define INTRO_TEXT_X 0
-#define INTRO_TEXT_Y 19
+/* U4 繁中化:中文故事/吉普賽提問換行較多(7–8 行),原 6 列文字區會溢出。
+   文字區底部仍對齊 400px,起點上移 19→17 → 8 列容納中文段落(配合
+   textview.cpp 的防越界 clip,徹底解決 intro 第 3 段 7 行崩潰)。 */
+#define INTRO_TEXT_Y 17
 #define INTRO_TEXT_WIDTH 40
-#define INTRO_TEXT_HEIGHT 6
+#define INTRO_TEXT_HEIGHT 8
 
 #define GYP_PLACES_FIRST 0
 #define GYP_PLACES_TWOMORE 1
@@ -661,7 +665,7 @@ void IntroController::drawBeastie(int beast, int vertoffset, int frame) {
     ASSERT(beast == 0 || beast == 1, "invalid beast: %d", beast);
 
     backgroundArea.draw(beastiesImg, beastieSub[beast] + frame,
-                        beast ? (320 - 48) : 0,
+                        beast ? (U4_SCREEN_W - 96) : 0,   // U4 繁中化 B:2x(48→96)
                         vertoffset);
 }
 
@@ -764,7 +768,7 @@ void IntroController::updateScreen() {
     case INTRO_MENU:
         // draw the extended background for all option screens
         backgroundArea.draw(BKGD_INTRO);
-        backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+        backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
         // if there is an error message to display, show it
         if (xu4.errorMessage)
@@ -780,7 +784,7 @@ void IntroController::updateScreen() {
             EventHandler::wait_msecs(3000);
             // clear the screen again
             backgroundArea.draw(BKGD_INTRO);
-            backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+            backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
         }
 
         menuArea.textAt(1,  1, "In another world, in a time to come.");
@@ -808,7 +812,7 @@ void IntroController::updateScreen() {
 void IntroController::initiateNewGame() {
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_INTRO);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
     // display name prompt and read name from keyboard
     menuArea.textAt(3, 3, "By what name shalt thou be known");
@@ -831,7 +835,7 @@ void IntroController::initiateNewGame() {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_INTRO);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
     // display sex prompt and read sex from keyboard
     menuArea.textAt(3, 3, "Art thou Male or Female?");
@@ -1103,7 +1107,7 @@ void IntroController::journeyOnward() {
 void IntroController::about() {
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_INTRO);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
     screenHideCursor();
     menuArea.textAtFmt(14, 1, "XU4 %s", VERSION);
@@ -1125,7 +1129,9 @@ void IntroController::about() {
  * Shows text in the question area.
  */
 void IntroController::showText(const string &text) {
-    string current = text;
+    /* U4 繁中化:intro 故事整段查表(key 含 \n),命中用中文再逐行渲染 */
+    const char* zh = chtLookup(text.c_str(), (int) text.length());
+    string current = zh ? zh : text;
     int lineNo = 0;
 
     questionArea.clear();
@@ -1270,7 +1276,7 @@ void IntroController::updateConfMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateVideoMenu(MenuEvent &event) {
@@ -1307,7 +1313,7 @@ void IntroController::updateVideoMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateGfxMenu(MenuEvent &event)
@@ -1327,7 +1333,7 @@ void IntroController::updateGfxMenu(MenuEvent &event)
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateSoundMenu(MenuEvent &event) {
@@ -1361,7 +1367,7 @@ void IntroController::updateSoundMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateInputMenu(MenuEvent &event) {
@@ -1389,7 +1395,7 @@ void IntroController::updateInputMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
     // after drawing the menu, extra menu text can be added here
     extendedMenuArea.textAt(0, 5, "Mouse Options:");
@@ -1420,7 +1426,7 @@ void IntroController::updateSpeedMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateGameplayMenu(MenuEvent &event) {
@@ -1444,7 +1450,7 @@ void IntroController::updateGameplayMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 }
 
 void IntroController::updateInterfaceMenu(MenuEvent &event) {
@@ -1468,7 +1474,7 @@ void IntroController::updateInterfaceMenu(MenuEvent &event) {
 
     // draw the extended background for all option screens
     backgroundArea.draw(BKGD_OPTIONS_TOP, 0, 0);
-    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 120);
+    backgroundArea.draw(BKGD_OPTIONS_BTM, 0, 240);
 
     // after drawing the menu, extra menu text can be added here
     extendedMenuArea.textAt(2, 3, "  (Open, Jimmy, etc.)");
@@ -1703,8 +1709,8 @@ void IntroController::initTitles()
 void IntroController::addTitle(int x, int y, int w, int h, AnimType method, int delay, int duration)
 {
     AnimElement data = {
-        x, y,               // source x and y
-        w, h,               // source width and height
+        x * 2, y * 2,       // U4 繁中化 B:2x(抽取/destImage/落點皆 640 space)
+        w * 2, h * 2,       // source width and height
         method,             // render method
         0,                  // animStep
         0,                  // animStepMax
@@ -1777,8 +1783,8 @@ void IntroController::getTitleSourceData()
                         color = info->image->setColor(255, (y == 2 ? 250 : 255), blue[y-1]);
                     }
                     AnimPlot plot = {
-                        uint8_t(x),
-                        uint8_t(y),
+                        int16_t(x * 2),     // U4 繁中化 B:2x 位置(漸層仍用 1x y)
+                        int16_t(y * 2),
                         uint8_t(color.r),
                         uint8_t(color.g),
                         uint8_t(color.b),
@@ -1800,15 +1806,15 @@ void IntroController::getTitleSourceData()
             {
                 for (int y=0; y < titles[i].rh; y++)
                 {
-                    // Here x is set to exclude PRESENT at top of image.
-                    int x = (y < 6) ? 133 : 0;
+                    // Here x is set to exclude PRESENT at top of image.(U4 繁中化 B:2x)
+                    int x = (y < 12) ? 266 : 0;
                     for ( ; x < titles[i].rw ; x++)
                     {
                         titles[i].srcImage->getPixel(x, y, r, g, b, a);
                         if (r || g || b)
                         {
                             AnimPlot plot = {
-                                uint8_t(x+1), uint8_t(y+1),
+                                int16_t(x+1), int16_t(y+1),
                                 uint8_t(r), uint8_t(g), uint8_t(b), uint8_t(a)
                             };
                             titles[i].plotData.push_back(plot);
@@ -1904,12 +1910,12 @@ bool IntroController::updateTitle()
         {
             while (animStepTarget > title->animStep)
             {
-                // blit the pixel-pair to the src surface
+                // blit the pixel-pair to the src surface(U4 繁中化 B:2x → 4x2)
                 title->destImage->fillRect(
                     title->plotData[title->animStep].x,
                     title->plotData[title->animStep].y,
+                    4,
                     2,
-                    1,
                     title->plotData[title->animStep].r,
                     title->plotData[title->animStep].g,
                     title->plotData[title->animStep].b);
diff --git a/src/intro.h b/src/intro.h
index c21ef93..f0c1c6a 100644
--- a/src/intro.h
+++ b/src/intro.h
@@ -257,7 +257,7 @@ private:
     };
 
     struct AnimPlot {
-        uint8_t x, y;
+        int16_t x, y;           // U4 繁中化 B:uint8→int16 容 640 座標
         uint8_t r, g, b, a;
     };
 
diff --git a/src/screen.cpp b/src/screen.cpp
index 46d51bd..70c9e3b 100644
--- a/src/screen.cpp
+++ b/src/screen.cpp
@@ -25,6 +25,7 @@
 #include "tileset.h"
 #include "xu4.h"
 #include "gpu.h"
+#include "cht.h"      // U4 繁中化 hook
 
 using std::vector;
 
@@ -189,6 +190,8 @@ static void screenInit_data(Screen* scr, Settings& settings) {
     if (! scr->charsetInfo)
         errorLoadImage(BKGD_CHARSET);
 
+    chtInit();      // 載入 en→zh lookup + CJK 字型
+
 #ifdef GPU_RENDER
     {
     ImageInfo* tinfo;
@@ -265,7 +268,7 @@ void screenInit(int layerCount) {
     xu4.screen = screen;
 
     // Create a special purpose image that represents the whole screen.
-    xu4.screenImage = Image::create(320, 200);
+    xu4.screenImage = Image::create(U4_SCREEN_W, U4_SCREEN_H);
     xu4.screenImage->fill(Image::black);
 
     screenInit_sys(xu4.settings, &screen->state, SYS_CLEAN);
@@ -350,6 +353,8 @@ bool screenLayerUsed(int layer) {
     return XU4_SCREEN->layers[layer].func ? true : false;
 }
 
+static void screenDrawCJKAt(const char* s, int cellX, int cellY);   // U4 繁中化前置宣告
+
 void screenTextAt(int x, int y, const char *fmt, ...) {
     char* buffer = XU4_SCREEN->msgBuffer;
     int i, buflen;
@@ -359,6 +364,10 @@ void screenTextAt(int x, int y, const char *fmt, ...) {
     buflen = vsnprintf(buffer, MsgBufferSize, fmt, args);
     va_end(args);
 
+    /* U4 繁中化(#4 HUD):查表命中則以 CJK 渲染在 (x,y) */
+    const char* zh = chtLookup(buffer, buflen);
+    if (zh) { screenDrawCJKAt(zh, x, y); return; }
+
     for (i = 0; i < buflen; i++)
         screenShowChar(buffer[i], x + i, y);
 }
@@ -403,6 +412,13 @@ void screenMessage(const char *fmt, ...) {
     if (! c)
         return; // Some cases (like the intro) don't have the context initiated.
 
+    /* U4 繁中化(#3 format-aware):先查 format 字串本身,命中則以中文 format
+     * 餵 vsnprintf —— 中文 zh format 與 en 的 %s/%d/%c 同序(已校驗),引數照常代入。
+     * 結果含 CJK,screenMessageN 會偵測 CJK bytes 改走 CJK 渲染。 */
+    const char* zhFmt = chtLookup(fmt, (int) strlen(fmt));
+    if (zhFmt)
+        fmt = zhFmt;
+
     va_list args;
     va_start(args, fmt);
     buflen = vsnprintf(buffer, MsgBufferSize, fmt, args);
@@ -446,7 +462,102 @@ void screenMessageCenter(const char* text, int newlines) {
     screenMessageN(start, cp - start);
 }
 
+/* U4 繁中化:buffer 是否含 UTF-8 多位元組(CJK)。 */
+static bool chtHasCJK(const char* s, int len) {
+    for (int i = 0; i < len; ++i)
+        if ((unsigned char) s[i] >= 0x80) return true;
+    return false;
+}
+
+/* U4 繁中化:把一個 CJK glyph(16×16 灰階 alpha)blit 到絕對像素 (px,py)。 */
+static void cjkBlitPx(uint32_t cp, int px, int py) {
+    const uint8_t* g = chtGlyph(cp);
+    int dim = chtGlyphDim();
+    Image* screen = xu4.screenImage;
+    if (! g) {
+        screen->fillRect(px, py, dim, dim, 60, 60, 60);   // 缺字:灰框
+        return;
+    }
+    for (int y = 0; y < dim; ++y)
+        for (int x = 0; x < dim; ++x) {
+            int a = g[y * dim + x];   // 灰階 alpha:白字混黑底(二值 atlas 亦相容)
+            screen->putPixel(px + x, py + y, a, a, a, 255);
+        }
+}
+
+/* 文字區 cell(col,line)→ 絕對像素 blit。 */
+static void cjkBlit(uint32_t cp, int col, int line) {
+    cjkBlitPx(cp, (TEXT_AREA_X + col) * CHAR_WIDTH, (TEXT_AREA_Y + line) * CHAR_HEIGHT);
+}
+
+/* U4 繁中化:把已翻譯 UTF-8 字串渲染到絕對 cell(cellX,cellY)起(單行,夾雜 ASCII)。
+ * 供 screenTextAt(HUD)使用。 */
+static void screenDrawCJKAt(const char* s, int cellX, int cellY) {
+    const unsigned char* p = (const unsigned char*) s;
+    int col = 0;
+    while (*p) {
+        unsigned char b = *p;
+        uint32_t cp; int n;
+        if (b < 0x80)            { cp = b;        n = 1; }
+        else if ((b >> 5) == 0x6){ cp = b & 0x1F; n = 2; }
+        else if ((b >> 4) == 0xE){ cp = b & 0x0F; n = 3; }
+        else if ((b >> 3) == 0x1E){cp = b & 0x07; n = 4; }
+        else { ++p; continue; }
+        for (int k = 1; k < n; ++k) {
+            if ((p[k] & 0xC0) != 0x80) { n = k; break; }
+            cp = (cp << 6) | (p[k] & 0x3F);
+        }
+        p += n;
+        if (cp < 0x80) { screenShowChar((int) cp, cellX + col, cellY); col += 1; }
+        else { cjkBlitPx(cp, (cellX + col) * CHAR_WIDTH, cellY * CHAR_HEIGHT); col += 1; }
+    }
+}
+
+/* U4 繁中化:把已翻譯的 UTF-8 中文字串渲染到文字區(CJK-aware,行距 2)。 */
+static void screenMessageCJK(const char* s) {
+    const int colCount = TEXT_AREA_W;
+    const unsigned char* p = (const unsigned char*) s;
+    if (c->line >= TEXT_AREA_H) { c->line = TEXT_AREA_H - 1; screenScrollMessageArea(); }
+    while (*p) {
+        unsigned char b = *p;
+        if (b == '\n') {
+            c->col = 0; c->line += 1;
+            while (c->line >= TEXT_AREA_H) { c->line--; screenScrollMessageArea(); }
+            ++p; continue;
+        }
+        uint32_t cp; int n;
+        if (b < 0x80)            { cp = b;        n = 1; }
+        else if ((b >> 5) == 0x6){ cp = b & 0x1F; n = 2; }
+        else if ((b >> 4) == 0xE){ cp = b & 0x0F; n = 3; }
+        else if ((b >> 3) == 0x1E){cp = b & 0x07; n = 4; }
+        else { ++p; continue; }
+        for (int k = 1; k < n; ++k) {
+            if ((p[k] & 0xC0) != 0x80) { n = k; break; }
+            cp = (cp << 6) | (p[k] & 0x3F);
+        }
+        p += n;
+        if (cp < 0x80) {                          // 夾雜 ASCII
+            if (c->col >= colCount) { c->col = 0; c->line += 1;
+                while (c->line >= TEXT_AREA_H) { c->line--; screenScrollMessageArea(); } }
+            screenShowChar((int) cp, TEXT_AREA_X + c->col, TEXT_AREA_Y + c->line);
+            c->col++;
+        } else {                                  // CJK 全形,佔 2 欄
+            if (c->col + 1 > colCount) { c->col = 0; c->line += 1;
+                while (c->line >= TEXT_AREA_H) { c->line--; screenScrollMessageArea(); } }
+            cjkBlit(cp, c->col, c->line);
+            c->col += 1;
+        }
+    }
+    screenSetCursorPos(TEXT_AREA_X + c->col, TEXT_AREA_Y + c->line);
+}
+
 void screenMessageN(const char* buffer, int buflen) {
+    /* U4 繁中化:load-time 查表命中,或 buffer 已含 CJK(來自 #3 format-aware
+     * 在 screenMessage 端翻好的 format 字串)→ 走 CJK 渲染 */
+    const char* zh = chtLookup(buffer, buflen);
+    if (zh) { screenMessageCJK(zh); return; }
+    if (chtHasCJK(buffer, buflen)) { screenMessageCJK(buffer); return; }
+
     bool colorize = xu4.settings->enhancements &&
                     xu4.settings->enhancementsOptions.textColorization;
     const int colCount = TEXT_AREA_W;
@@ -850,7 +961,44 @@ static void screenUploadCursor(Screen* sp, uint32_t stex) {
                     &cimg);
 }
 
+/* U4 繁中化 self-test(env U4CHT_SELFTEST):headless 驗證
+ * 資產載入→真實 chtLookup→CJK glyph blit→上傳 全鏈路。每幀覆寫到文字區。 */
+static void chtSelfTest() {
+    if (! getenv("U4CHT_SELFTEST")) return;
+    static const char* keys[] = {
+        "a charming bard.",
+        "Magincia was\ndestroyed by its\npride.",
+    };
+    int line = 0;
+    for (int ki = 0; ki < 2; ++ki) {
+        const char* en = keys[ki];
+        const char* zh = chtLookup(en, (int) strlen(en));
+        if (! zh) { line += 1; continue; }
+        int col = 0;
+        const unsigned char* p = (const unsigned char*) zh;
+        while (*p) {
+            unsigned char b = *p;
+            if (b == '\n') { col = 0; line += 1; ++p; continue; }
+            uint32_t cp; int n;
+            if (b < 0x80)             { cp = b;        n = 1; }
+            else if ((b >> 5) == 0x6) { cp = b & 0x1F; n = 2; }
+            else if ((b >> 4) == 0xE) { cp = b & 0x0F; n = 3; }
+            else if ((b >> 3) == 0x1E){ cp = b & 0x07; n = 4; }
+            else { ++p; continue; }
+            for (int k = 1; k < n; ++k) { if ((p[k] & 0xC0) != 0x80) { n = k; break; }
+                cp = (cp << 6) | (p[k] & 0x3F); }
+            p += n;
+            if (cp < 0x80) { screenShowChar((int) cp, TEXT_AREA_X + col, TEXT_AREA_Y + line); col++; }
+            else { if (col + 1 > TEXT_AREA_W) { col = 0; line += 1; }
+                cjkBlit(cp, col, line); col += 1; }
+        }
+        line += 1;
+    }
+    XU4_SCREEN->uploadScreen = 1;
+}
+
 void screenRender() {
+    chtSelfTest();
     Screen* sp = XU4_SCREEN;
     void* gpu = xu4.gpu;
     ScreenState* ss = &sp->state;
diff --git a/src/textview.cpp b/src/textview.cpp
index eb6fcc0..8b051af 100644
--- a/src/textview.cpp
+++ b/src/textview.cpp
@@ -3,6 +3,7 @@
  */
 
 #include <stdarg.h>
+#include <cstring>
 
 #include "debug.h"
 #include "imagemgr.h"
@@ -11,6 +12,8 @@
 #include "textview.h"
 #include "u4.h"
 #include "xu4.h"
+#include "cht.h"      // U4 繁中化
+#include "image.h"
 
 using std::string;
 
@@ -161,6 +164,53 @@ string TextView::highlightKey(const string& input, unsigned int keyIndex) {
 }
 
 void TextView::textAt(int x, int y, const char *text) {
+    /* U4 繁中化(#4 intro 選單 / 角色創建):查表命中或字串已含 CJK → CJK 渲染 */
+    const char* zh = chtLookup(text, (int) strlen(text));
+    const char* render = zh ? zh : text;
+    bool hasCJK = false;
+    for (const char* q = render; *q; ++q)
+        if ((unsigned char) *q >= 0x80) { hasCJK = true; break; }
+    if (hasCJK) {
+        const unsigned char* p = (const unsigned char*) render;
+        int col = 0, dim = chtGlyphDim();
+        while (*p) {
+            unsigned char b = *p;
+            uint32_t cp; int n;
+            if (b < 0x80)            { cp = b;        n = 1; }
+            else if ((b >> 5) == 0x6){ cp = b & 0x1F; n = 2; }
+            else if ((b >> 4) == 0xE){ cp = b & 0x0F; n = 3; }
+            else if ((b >> 3) == 0x1E){cp = b & 0x07; n = 4; }
+            else { ++p; continue; }
+            for (int k = 1; k < n; ++k) {
+                if ((p[k] & 0xC0) != 0x80) { n = k; break; }
+                cp = (cp << 6) | (p[k] & 0x3F);
+            }
+            p += n;
+            if (cp < 0x80) { drawChar((int) cp, x + col, y); col += 1; }
+            else {
+                const uint8_t* g = chtGlyph(cp);
+                int px = this->x + (x + col) * CHAR_WIDTH;
+                int py = this->y + y * CHAR_HEIGHT;
+                /* U4 繁中化:防越界 clip。putPixel/fillRect 不做邊界檢查,
+                   中文翻譯換行較多時 y 可能超出文字區 → 越界寫入 screenImage
+                   → segfault(如 intro 故事第 3 段 7 行溢出 6 列文字區)。
+                   以 screenImage 邊界裁切,徹底根治此類崩潰。 */
+                const int SW = xu4.screenImage->width();
+                const int SH = xu4.screenImage->height();
+                if (g) for (int yy = 0; yy < dim; ++yy) for (int xx = 0; xx < dim; ++xx) {
+                    int dx = px + xx, dy = py + yy;
+                    if (dx < 0 || dy < 0 || dx >= SW || dy >= SH) continue;
+                    int a = g[yy * dim + xx];
+                    xu4.screenImage->putPixel(dx, dy, a, a, a, 255);
+                } else if (px >= 0 && py >= 0 && px + dim <= SW && py + dim <= SH) {
+                    xu4.screenImage->fillRect(px, py, dim, dim, 60, 60, 60);
+                }
+                col += 1;
+            }
+        }
+        return;
+    }
+
     int ch;
     while ((ch = *text++)) {
         switch (ch) {
@@ -203,6 +253,9 @@ void TextView::textAtFmt(int x, int y, const char *fmt, ...) {
  * Draw text with a single character highlighted.
  */
 void TextView::textAtKey(int x, int y, const char *text, int keyIndex) {
+    /* U4 繁中化:命中則直接以中文渲染(略過 key 高亮);未命中走原流程 */
+    const char* zh = chtLookup(text, (int) strlen(text));
+    if (zh) { textAt(x, y, zh); return; }
     string ctext = highlightKey(text, keyIndex);
     textAt(x, y, ctext.c_str());
 }
diff --git a/src/textview.h b/src/textview.h
index 8bfa3de..ba90b57 100644
--- a/src/textview.h
+++ b/src/textview.h
@@ -11,8 +11,8 @@
 #define PRINTF_LIKE(x,y)
 #endif
 
-#define CHAR_WIDTH 8
-#define CHAR_HEIGHT 8
+#define CHAR_WIDTH 16
+#define CHAR_HEIGHT 16
 
 #include <string>
 #include "view.h"
diff --git a/src/u4.h b/src/u4.h
index 767f30a..57f22e4 100644
--- a/src/u4.h
+++ b/src/u4.h
@@ -24,8 +24,8 @@
 #define TILE_HEIGHT (2 * CHAR_HEIGHT)
 
 /* info for loading image data from charset.ega */
-#define CHAR_WIDTH 8
-#define CHAR_HEIGHT 8
+#define CHAR_WIDTH 16
+#define CHAR_HEIGHT 16
 
 /* some character defines */
 #define CHARSET_ANKH '\0'
@@ -47,16 +47,16 @@
 #define CHARSET_FLOOR '\022'
 
 /* screen size (in pixels) */
-#define U4_SCREEN_W 320
-#define U4_SCREEN_H 200
+#define U4_SCREEN_W 640
+#define U4_SCREEN_H 400
 
 /* map viewport size (in tiles) */
 #define VIEWPORT_W 11
 #define VIEWPORT_H 11
 
 /* screen border size (in pixels) */
-#define BORDER_WIDTH 8
-#define BORDER_HEIGHT 8
+#define BORDER_WIDTH 16
+#define BORDER_HEIGHT 16
 
 /* text area (in character units) */
 #define TEXT_AREA_X 24
