FROM ubuntu:24.04
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends \
      mame-tools ffmpeg p7zip-full unzip mtools dosfstools python3 python3-pil \
      ca-certificates xxd binutils \
 && rm -rf /var/lib/apt/lists/*
WORKDIR /work
